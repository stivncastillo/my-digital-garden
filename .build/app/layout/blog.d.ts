import React from 'react';
declare type Props = {
    children: React.ReactNode;
    frontMatter: FrontMatter;
};
declare const BlogLayout: ({ children, frontMatter }: Props) => JSX.Element;
export default BlogLayout;
