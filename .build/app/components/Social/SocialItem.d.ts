import React from 'react';
declare type Props = {
    children: React.ReactNode;
    name: string;
    href: string;
};
declare const SocialItem: ({ children, name, href }: Props) => JSX.Element;
export default SocialItem;
